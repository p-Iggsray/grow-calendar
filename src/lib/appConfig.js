// App-wide static constants. Sub-project B (#92) will move location into
// per-user plan data; until then this is the single source of truth, shared by
// the worker (MJ persona) and the React app (header, login).
export const LOCATION = "Athens, Ohio";
